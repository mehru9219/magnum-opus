import { defineSchema } from "convex/server";

// Schema will be populated by Agent 1 (Database Schema Architect)
// This is a placeholder to allow Convex initialization

const schema = defineSchema({
  // Tables will be added by Agent 1
});

export default schema;
